---
- name: Install dependencies for adding apt repo
  apt:
    name:
      - apt-transport-https
      - gnupg
      - curl
    state: present

- name: Add Elastic GPG key
  apt_key:
    url: https://artifacts.elastic.co/GPG-KEY-elasticsearch
    state: present

- name: Add Elastic 8.x apt repository
  apt_repository:
    repo: "deb https://artifacts.elastic.co/packages/8.x/apt stable main"
    state: present
    filename: elastic-8.x

- name: Install Elasticsearch
  apt:
    name: "elasticsearch={{ es_version }}"
    state: present
    update_cache: yes

- name: Deploy elasticsearch.yml
  template:
    src: elasticsearch.yml.j2
    dest: /etc/elasticsearch/elasticsearch.yml
    owner: root
    group: elasticsearch
    mode: "0660"
  notify: restart elasticsearch

- name: Deploy JVM heap options
  template:
    src: jvm.options.j2
    dest: /etc/elasticsearch/jvm.options.d/heap.options
    owner: root
    group: elasticsearch
    mode: "0644"
  notify: restart elasticsearch

- name: Enable Elasticsearch service (do not start yet)
  systemd:
    name: elasticsearch
    enabled: yes
    daemon_reload: yes
